{% extends 'layouts/main.html.twig' %}


{% block title %} {{ post.title }} - {{ parent() }}{% endblock %}

{% block body %}

    <h1>{{ post.title }}</h1>
    <p>{{ post.content }}</p>

{% endblock %}